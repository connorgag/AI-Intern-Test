# AI-Intern-Test

Run neo4j with:
```brew services start neo4j```

To the same thing for Influx:
```
influx auth create \
  --org none \
  --all-access
```
and to start: 
```brew services start influxdb```